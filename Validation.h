#include"iostream"
#include<cctype>
#include<algorithm>

using namespace std;

bool isValidString(const string& input);
bool isValidNumber(const string& input);
bool isWithinRage(int number, int min, int max);
string convertToLower(const string& str);